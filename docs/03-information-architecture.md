# Arquitectura de información

## Público

`/`, `/servicios/aire-acondicionado`, `/como-funciona`, `/ayuda`, `/login`, `/registro`

## Cliente

`/app`, `/app/solicitar/aire-acondicionado`, `/app/solicitudes`, `/app/trabajos`, `/app/equipos`, `/app/direcciones`, `/app/perfil`, `/app/pagos`

## Profesional

`/pro`, `/pro/onboarding/[token]`, `/pro/dashboard`, `/pro/solicitudes`, `/pro/trabajos`, `/pro/agenda`, `/pro/equipos/[id]`, `/pro/pagos`, `/pro/perfil`, `/pro/mercadopago`

## Admin

`/admin`, `/admin/dashboard`, `/admin/solicitudes`, `/admin/trabajos`, `/admin/profesionales`, `/admin/clientes`, `/admin/equipos`, `/admin/pagos`, `/admin/precios`, `/admin/servicios`, `/admin/diagnostico`, `/admin/calidad`, `/admin/configuracion`, `/admin/auditoria`
